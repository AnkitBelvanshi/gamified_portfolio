export const metadata = {
  title: "Mission Dispatch Center | ANKIT.DEV",
  description: "Contact Ankit Belvanshi — reach out via GitHub, LinkedIn, or email. Submit a quest request for collaboration.",
};

export default function ContactLayout({ children }) {
  return children;
}
